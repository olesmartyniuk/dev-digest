/** Layout constants for the "Create skill from conventions" modal. */
export const MODAL_WIDTH = 760;

/** Rows for the skill-body editor — tall enough to read a full rule section. */
export const BODY_ROWS = 16;
